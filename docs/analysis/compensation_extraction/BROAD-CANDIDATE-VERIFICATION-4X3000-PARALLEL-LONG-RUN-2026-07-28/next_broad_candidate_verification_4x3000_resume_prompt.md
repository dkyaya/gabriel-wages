# Next task prompt

Resume only `verify_lane_004` against its unchanged 2,144-row locked queue and queue hash. Do not rerun completed lanes 001–003. First require escalated network permission and pass a lane-local HEAD-only smoke; stop and quarantine any uniform `ConnectError` attempt. Preserve the quarantined sandbox-denied attempt only as audit history and never count it as verification. Do not run candidate review, download, source review, or source-content inspection. After lane 004 completes, rerun the coordinator, update dashboard/status/docs from valid merged outcomes, keep the map total scout coverage only, and keep global analysis readiness false. Future rating tasks must verify downstream artifact completeness and deterministically reconstruct derivable missing summaries before closure.
