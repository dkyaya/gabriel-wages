# Readiness gate stress-test report

The gate fails closed on count drift, quarantine leakage, missing non-derivable inputs, uncontrolled statuses, claim-boundary drift, or an ambiguous true overall flag. Rebuilding into a temporary directory must reproduce the decision, flags, and reconciliation byte-for-byte.
