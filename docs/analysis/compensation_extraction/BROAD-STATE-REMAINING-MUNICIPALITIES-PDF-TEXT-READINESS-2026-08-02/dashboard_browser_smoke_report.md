# Dashboard browser smoke report

Status: **passed by production/relative build and static-bundle contract; visual browser unavailable**.

Both dashboard builds passed. The generated bundle contains the completed PDF/text-readiness stage, the 2,558-row text-extraction-ready count, the 2,176 PDF and 382 HTML ready counts, the text-extraction next task, `scout_coverage_rate`, the final PI report link, the wage-growth continuity module, and the collapsed technical-audit section. No final national wage-gap, prevalence, or causal claim was found.

The in-app browser runtime reported that no browser was available. Its prescribed bootstrap-troubleshooting document could not be read because the installed plugin documentation path was version-skewed. The local preview process also ran in an isolated execution session and was not reachable from a separate command. Accordingly, no visual-browser or local cross-process HTTP pass is claimed.
