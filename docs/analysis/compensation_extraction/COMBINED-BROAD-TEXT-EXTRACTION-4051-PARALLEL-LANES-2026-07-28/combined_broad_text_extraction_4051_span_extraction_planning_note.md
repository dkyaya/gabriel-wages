# Deterministic span-extraction planning

The next bounded queue contains only the 3,815 extracted-ok rows with hash-valid ignored full-text artifacts. Empty, low-density, bad-layer, noisy, unsupported, and error rows are excluded. The next stage may capture exact verbatim evidence spans but must not rate, ingest, codify, normalize wages, or make causal or prevalence claims.
