# Dashboard overview metric sync

The coordinator supplied all span metrics to the dashboard builder. The builder completed successfully, the current phase is `combined_broad_span_extraction_3815_completed_rating_ready`, all overview counts reconcile to the merged ledger, the map remains total scout coverage only, and global analysis readiness is false. Frontend build validation is recorded in the task validation report.
