# Deterministic field rule registry

Version: `deterministic-field-rules-2026-08-05-v1`. The registry contains 205 explicit, locally auditable label/header rules. Rules preserve literals and coordinates; they do not perform semantic imputation, pay-base conversion, matching, or GABRIEL scoring.
