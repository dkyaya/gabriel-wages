# Dashboard stale overview guard

The prior exact-span-rating-complete operation cannot remain current after this summary. Evidence filters cannot alter or replace the total scout coverage map filter.
