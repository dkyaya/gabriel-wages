# Combined broad rating ingestion/codification result

Decision `combined_broad_rating_ingestion_codification_16947_completed_global_gate_ready`. The committed package contains **16,947** ingested/codified valid rating records across four reconciled lanes, with **312** quarantines excluded. The dashboard now points to a dedicated global-readiness diagnostic gate as next. Map coverage remains total scout coverage only; global analysis readiness remains false.
