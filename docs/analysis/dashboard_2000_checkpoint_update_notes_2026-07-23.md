# Dashboard 2,000-Checkpoint Update Notes

Date: 2026-07-23

## Outcome

The static project hub now presents the PI-aligned phase as **Source Discovery Scale-Up**: continue coordinated ordinary 150-municipality waves from 794 successful coverage to an approximately 2,000-covered workflow checkpoint, then pause broad scouting for verification, extraction, ingestion, source rating, and descriptive wage-gap analysis.

## Dashboard sections updated

- Overview/header: active phase and approximately 2,000-covered checkpoint replace the former breadth-versus-verification decision framing.
- Project Phase: a compact progress panel shows 794/2,000, 1,206 remaining, estimated 8–9 waves, candidate and failure-lane metrics, the post-checkpoint sequence, and deferred regressions.
- Verification Pipeline: verification is positioned as the first post-checkpoint downstream gate rather than the immediate recommended 50–100-row pilot.
- Analysis Readiness: renamed and expanded as **Descriptive Wage-Gap Analysis, Planned**, with the proposed descriptive gap concept, future map/filter capability, mechanism-correlation documentation, and explicit unavailability before verification/extraction.
- Next Steps: ordinary Tier-prioritized 150-row discovery is the immediate task; failure-only retries remain separate.
- Navigation: added direct entries for Project Phase and Analysis Plan while preserving map, reports, definitions, state hash routing, and responsive behavior.

## JSON added or updated

- Added `docs/dashboard/data/project_phase_summary.json`, generated by `scripts/build_dashboard_data.py` from current state coverage and candidate queue artifacts.
- Updated generated `scout_operations_summary.json` with the active checkpoint strategy.
- Updated generated `scout_runtime_trends.json` with checkpoint context.
- Rebuilt the existing dashboard and scout-yield JSON layers from existing committed artifacts only.

The new phase JSON records the source-accounting checkpoint commit `3f2f815f4ca4b4e90f6ca1bff769bd300843d703`. It does not imply that candidate leads were verified.

## Caveats

- Candidate rows remain unverified source leads.
- Scout-covered municipalities are not verified or analysis-ready municipalities.
- The approximately 2,000-covered value is a workflow pause point, not an evidentiary threshold.
- Wage-growth gaps have not been calculated.
- Mechanisms have not been analyzed for correlation with wage-growth gaps.
- Priority tiers remain operational scheduling inputs, not findings.
- Regressions are deferred.

## Accounting boundary

No national queue, coverage, priority-tier, or source-discovery input was changed. Dashboard/report JSON was rebuilt only from existing committed artifacts. No scout, worker dry-run, preflight, API/model call, hosted search, URL verification, source ingestion, `gabriel.codify`, candidate promotion, wage-gap finding, or causal claim occurred.

## Future map/filter requirement

After verified and extracted matched wage observations exist, the dashboard should add a wage-growth-gap percentage map layer and range filter. The layer must expose comparison occupation, time window, wage concept, source quality, matched-set provenance, sample size, and missingness; it must never display unavailable gaps as zero or imply causal effects.
