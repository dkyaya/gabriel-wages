# Excluded asset summary

The clean repository excludes raw source binaries, extracted-text corpora, source archive parts, millions-row field/span/observation layers, large reconciliation and normalization outputs, worker queues, checkpoints, logs, caches, task folders, old prompts and relays, old atlas editions, old dashboard builds, one-time repair scripts, and original Git history. The external source release and the historical original repository preserve the material that does not belong in normal handoff use.
