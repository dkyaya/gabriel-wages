# Source-library linkage QA

Pass. The clean repo carries the complete 26,635-row source index, final release and volume manifests, all 28 archive checksums, schemas, a small alias table, and source-family/extraction-status dictionaries. The external corpus is configured through `tools/configure_source_library.py`; status, quick/full verification, offline index search, and safe 28-part extraction tools are present. The exact recommended relative command passed against a valid bounded fixture. Machine-local state is written only to ignored `config/local_paths.toml`.
