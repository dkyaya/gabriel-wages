# Next task

## User transfer and approval step

Before destructive cleanup:

1. Create and push the clean private GitHub repository using the clean repo's `GITHUB_RELEASE_INSTRUCTIONS.md`.
2. Upload to durable private storage:
   - `gabriel-wages-original-git-history-2026-08-07.bundle`
   - `gabriel-wages-summer-project-record-2026-08-07.zip`
   - preferably the clean-repo bundle and source snapshot
   - `FINAL_HANDOFF_ARCHIVE_CHECKSUMS.sha256`
3. Independently verify the uploaded/downloaded archive hashes.
4. Review `LOCAL_CLEANUP_PREVIEW.md`, `cleanup_strategy_comparison.md`, `cleanup_wave_plan.md`, and `irreplaceable_artifact_audit.csv`.
5. Explicitly state whether Strategy A or Strategy B is approved and resolve the manual-review items.

Only then recommend:

`GABRIEL-WAGES-APPROVED-LOCAL-PROJECT-CLEANUP-2026-08-07`

That task must regenerate and freeze exact paths, stop on any mismatch, checkpoint each approved wave, measure reclaimed bytes, preserve the clean repo and external source library, and never exceed the approved cleanup classes.
