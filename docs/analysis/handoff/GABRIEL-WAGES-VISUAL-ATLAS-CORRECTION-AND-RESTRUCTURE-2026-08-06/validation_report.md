# Validation report

PASS LOCALLY; PUBLIC VALIDATION BLOCKED EXTERNALLY — map, event-unit, claim, caption, terminology, limitation, methodology, PDF, preservation, dashboard-build, and source-policy checks pass. The atlas commit was pushed, but GitHub reported a major Actions and Pages outage. Workflow run `31124212169` remained queued without a runner, so the new landing page and served-PDF checksum await a deployment-only resume.
