# Four-lane targeted scouting staggered live result

Decision: `targeted_scouting_four_lane_staggered_live_preflight_failed`. All locked-input integrity checks passed, but live execution failed closed before any hosted call because the fixed stagger offsets conflict with the no-simultaneous-lanes requirement. See the output directory for the preflight audit and repair prompt. Global analysis readiness remains false.
