# Dashboard visibility check for the bounded memo and Tier C source review

Status: repo-level deployment fix complete; the production Pages artifact will be regenerated by the push workflow.

The dashboard deployment source is GitHub Actions, not a checked-in `gh-pages` branch or a directly served `docs/` directory. `.github/workflows/deploy-dashboard.yml` rebuilds `docs/dashboard/data/`, builds the Vite application with the `/gabriel-wages/` base path, uploads `docs/dashboard/dist/`, and deploys that artifact to GitHub Pages.

The stale public header was caused by a local data-contract mismatch. The current header read historical scout fields whose last data vintage was `2026-07-23`, even though memo and Tier C metadata were already present elsewhere in the generated dashboard data. The dashboard now reads the generated current-phase summary.

The rebuilt local deploy artifact reports:

- data vintage: `2026-07-27`;
- current phase: `Dashboard fixed; Tier C source review complete; PDF/text-layer readiness ready`;
- memo decision: `bounded_internal_mechanism_linkage_claim_memo_completed_tier_c_verification_recommended`;
- Tier C verification decision: `targeted_tier_c_verification_completed_source_review_ready_dashboard_visible`;
- exact same-source memo scope: 268 linked pairs, 208 quantitative rows, and 90 qualitative mechanism records;
- verified Tier C source leads: 556;
- retained Tier C sources: 463;
- global analysis readiness: false; and
- PDF/text-layer readiness review ready next: true.

The exact current marker `Scaled verification routing and source triage` and rendered header `Data vintage 2026-07-23` are absent from the rebuilt production bundle. Historical `2026-07-23` identifiers remain in archived scout-round metadata and are not presented as the current phase or vintage.

Detailed diagnostics and the exact remote deployment IDs are recorded in the task-local `dashboard_remote_pages_diagnostics.*`, `dashboard_deployment_source_mapping.md`, `dashboard_stale_marker_scan.md`, and `dashboard_current_metadata_visibility_check.json` outputs. No remote settings were changed. Browser cache or GitHub Pages propagation may briefly delay external visibility after the successful deployment workflow completes.
