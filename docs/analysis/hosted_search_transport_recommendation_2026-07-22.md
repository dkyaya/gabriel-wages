# Hosted-Search Transport Recommendation — 2026-07-22

## Recommendation A — hosted-search route healthy now

The no-search control and both search-enabled diagnostics passed through the same direct-SDK production transport function. Each returned a response ID, nonempty text, and positive output tokens. The Oklahoma City municipality-style diagnostic also satisfied its requested JSON shape. A subsequent one-row production-runner probe completed and parsed successfully.

The earlier stopped attempts at commits `c6b3664` and `25445fe` should therefore remain classified as infrastructure transport incidents, not municipality failures and not source-yield evidence. Oklahoma City and Phoenix must remain ordinary unscouted Tier 1 targets in national accounting until a merge-eligible scout run occurs.

## Recommended next action

Under a new, explicit live-scout authorization, retry the locked Tier 1 150-row input in a fresh output directory with lineage to both stopped attempts and this diagnostic. Before the full run:

1. Reconfirm the locked SHA-256 `77b66569bcc2803e5067f84ad20b63e595f8c0611beb87820166b1a3a9de112b` and current eligibility.
2. Use a fresh no-search smoke immediately before live execution.
3. Keep direct SDK, `--state ALL --allow-mixed-states`, `--live-hard-cap 150`, `--max-prompts 150`, `--n-parallels 1`, five-second spacing, a 90-second timeout, and zero SDK retries.
4. Stop on two consecutive no-ID/no-text/no-token transport failures and preserve partial artifacts.
5. Never reuse either stopped output directory or the diagnostic directories.
6. Merge queue/coverage exactly once only after a complete merge-eligible run.

The Category A result supports a retry; it does not authorize one in this task and does not guarantee sustained service health. If the next fresh smoke or early live calls reproduce the immediate connection pattern, stop and escalate the sanitized route/timestamp evidence to HUIT rather than looping retries.

## One-row probe disposition

The diagnostic-only Oklahoma City probe demonstrated that the actual full scout prompt and parser can complete now. Its two candidate rows remain quarantined inside the probe artifact directory and must not be added to the national queue, coverage, priority evidence, corpus, or claims. The probe does not mark Oklahoma City covered; the later full wave should still include it.
