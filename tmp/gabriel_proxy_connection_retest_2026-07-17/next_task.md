# Recommended next task

Do not run a Massachusetts or other state scout yet. Provide HUIT the current exact GABRIEL error, `['Connection error.']`, and the non-secret metadata in `docs/analysis/gabriel_proxy_connection_retest_2026-07-17.md`.

If HUIT reports the proxy/API path restored, obtain separate authorization for exactly one new synthetic no-search smoke test first. Only if it produces a nonempty successful response (and a response ID when exposed by GABRIEL) should the separately authorized locked eight-row Massachusetts command in the retest note be considered.
