# Next task: limited exact-span qualitative analysis-facing promotion

Do not run this task without new explicit user authorization.

The current decision is `limited_exact_span_qualitative_readiness_pass_with_blockers_documented`. Prepare a rollback-safe provisional limited qualitative promotion layer from only the 759-row `exact_span_coded_candidate` tier. This prompt remains authorization-gated and must not be run without new explicit user authorization.

Preserve the 614 ambiguous and 581 unavailable rows as navigation-only; do not include them as coded evidence. Preserve all 759 exact spans, historical and span QA fields, page pointers, hashes, identifiers, source lineage, and tier fields. Add explicit limited-use eligibility and quarantine fields rather than dropping rows. Quarantine the 93 `needs_review` rows, eight `mechanism_type=other` rows, four rows without structured mechanism detail, and 16 historical mixed memberships. Restrict rows without exact cycle, controlled occupation, or matched-set support from the corresponding analysis scope; only 85 rows have exact matched-set support for the primary city-by-cycle design.

Keep quantitative, non-base, reference/control, and two unresolved groups/five observations separate. Keep analysis readiness false and stop before ingestion, codification, wage-gap calculations, regressions, or causal interpretation. Use the complete prepared prompt in the readiness-review output directory.
