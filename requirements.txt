# Core validator: stdlib only (no install needed)
# Ingestion layer:
pdfplumber>=0.10
pypdf>=4.0
pdf2image>=1.17
pytesseract>=0.3.10
reportlab>=4.0
# Optional LLM span fallback:
anthropic>=0.39
# System packages (not pip): poppler-utils, tesseract-ocr
